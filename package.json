{
  "name": "credit-card-mgmt-root",
  "version": "1.0.0",
  "description": "Root for Credit Card Sales Management System",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "npm run install-all && cd frontend && npm run build --emptyOutDir",
    "start": "cd backend && node server.js",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
